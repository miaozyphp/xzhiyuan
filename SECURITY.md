# Security Policy

## Supported versions

安全修复只提供给最新发布版本。旧版本用户应先升级并确认问题是否仍然存在。

| Version | Supported |
| --- | --- |
| Latest release | Yes |
| Older releases | No |

## Reporting a vulnerability

仓库公开后，请优先使用 GitHub 仓库 `Security` 页面中的私密漏洞报告功能。不要在公开 Issue、讨论区、日志附件或截图中披露利用步骤、访问令牌、个人路径或用户内容。

如果私密报告功能暂不可用，可以创建一个不含漏洞细节的 Issue，请维护者提供私密联系方式。报告建议包含受影响版本、Windows 与 Codex 版本、影响范围、最小复现步骤和建议修复方向。维护者应尽量在 7 天内确认收到报告，并在修复可用后再协调公开披露。

## Security model

- x纸鸢以当前用户权限运行，不需要管理员权限。
- Codex 调试端口仅绑定 `127.0.0.1`，但同一台电脑上的其他进程仍可能访问它。
- 媒体服务仅绑定随机本地端口，并使用每次运行随机生成的路径令牌。
- 主题路径必须保持在应用数据目录内；相对路径和扩展名在导入及读取时都会校验。
- 运行时样式可移除，应用不会修改 Codex 安装包或 `app.asar`。
- 自动应用代理是可选项，并通过当前用户启动项注册。

特别关注的漏洞包括路径穿越、任意文件读取、非本地端口暴露、调试端口权限扩大、注入脚本逃逸、安装包供应链和不受控进程终止。普通兼容性故障、主题显示问题和第三方素材授权争议应使用常规 Issue 流程。
