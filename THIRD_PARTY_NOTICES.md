# Third-Party Notices

## Microsoft WebView2

x纸鸢 uses the Microsoft WebView2 SDK package. Its license and notices
are distributed by the NuGet package and included in build outputs as required.
The Windows setup package also carries Microsoft's signed Evergreen Bootstrapper
and runs it only when the WebView2 Runtime is not already installed.

## Lucide

The workbench bundles Lucide icons under the ISC License. The license text is
stored at `src/ThemeStudio.App/ui/vendor/LUCIDE-LICENSE.txt`.

## Inno Setup

Windows installers are compiled with Inno Setup. Its redistribution license is
stored at `installer/languages/Inno-Setup-License.txt`.

## Inno Setup Chinese Simplified Translation

`installer/languages/ChineseSimplified.isl` is the community-maintained Inno
Setup Simplified Chinese translation by Zhenghan Yang. Its original attribution
and project URL remain in the file header. The translation is used only while
building the Windows installer.
